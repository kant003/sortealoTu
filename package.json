{
  "name": "sortealotu",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^10.11.0",
    "vite": "^3.1.0",
    "vitest": "^0.24.5"
  }
}
